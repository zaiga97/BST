#include "bst.h"
#include <iostream>
#include <vector>
#include <memory>

//   insert()
/*
template<typename Tkey, typename Tvalue, typename Tcompare>
std::pair<typename BST<Tkey, Tvalue, Tcompare>::iterator, bool> BST<Tkey, Tvalue, Tcompare>::insert(std::pair<Tkey, Tvalue>&& x){
	//node new_node{x};

	std::pair<iterator, bool> ret =  (iterator{}, false);
	return ret;
}


template<typename Tkey, typename Tvalue, typename Tcompare>
std::pair<typename BST<Tkey, Tvalue, Tcompare>::iterator, bool> BST<Tkey, Tvalue, Tcompare>::insert(const std::pair<Tkey, Tvalue>& x){
	//node new_node{x};

	std::pair<iterator, bool> ret =  (iterator{}, false);
	return ret;
}
*/